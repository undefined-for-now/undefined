version: '3.7'

services:
  minigames-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    volumes:
      - ./backend:/app
    container_name: minigames-backend
    expose:
      - '4000'
    ports:
      - '4000:4000'